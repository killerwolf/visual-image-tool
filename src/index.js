/**
 * Main entry point for the image-tool package
 * Exports the VisualImageTool class
 */

import VisualImageTool from "./visual-image-tool.js";

// Export only the class as a named export
export { VisualImageTool };
